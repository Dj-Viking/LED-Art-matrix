//SKETCH ONE AND TWO ACTIONS
export const sketchOneOn = (data) => {
  return {
    type: 'SKETCH_ONE_ON',
    payload: data
  };
};
export const sketchTwoOn = (data) => {
  return {
    type: 'SKETCH_TWO_ON',
    payload: data
  };
};